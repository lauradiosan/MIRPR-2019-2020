# Driving Assistance Happy Flow

* The Happy Flow of the application. (GUI version)
* If you want to use a new package, add it in requirements.txt

## Run in terminal

* ```$ cd path/to/HappyFlowGUI```
* ```$ pip install -r requirements.txt```
